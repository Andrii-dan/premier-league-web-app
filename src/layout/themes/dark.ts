import { createTheme } from '@mui/material';

const dark = createTheme({
	palette: {
		mode: 'dark',
		primary: {
			main: '#973CFF',
		},
		secondary: {
			main: '#FFD94A',
		},
	},
});

export default dark;
